{
  "name": "rex-cloud-backend",
  "version": "3.0.0",
  "type": "module",
  "description": "Backend API for REX Cloud - Excel schedule import via Upstash Redis",
  "scripts": { "dev": "vercel dev" },
  "dependencies": {
    "@upstash/redis": "^1.38.0"
  },
  "engines": { "node": "22.x" }
}
