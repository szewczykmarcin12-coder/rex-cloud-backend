{
  "name": "rex-cloud-backend",
  "version": "3.0.0",
  "type": "module",
  "description": "Backend API for REX Cloud - Excel schedule import via Vercel KV",
  "scripts": { "dev": "vercel dev" },
  "dependencies": {
    "@vercel/kv": "^2.0.0"
  },
  "engines": { "node": "20.x" }
}
